#include <iostream>
#include <vector>

using namespace std;

int main() {
    int T, N, M, a, b;
    cin >> T;
    for (int t = 0; t < T; t++) {
        cin >> N >> M;
        for (int i = 0; i < M; i++) {
            cin >> a >> b;

        }
        cout << N - 1 << '\n';
    }
}