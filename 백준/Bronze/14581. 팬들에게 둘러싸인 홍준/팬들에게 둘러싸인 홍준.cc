#include <iostream>
#include <algorithm>
#include <cmath>
#include <string.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	string name; cin >> name;

	cout << ":fan::fan::fan:" << '\n' << ":fan::" << name << "::fan:" << '\n' << ":fan::fan::fan:";
}